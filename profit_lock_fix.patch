diff --git a/core/backtester.py b/core/backtester.py
index 78517b5..9851776 100644
--- a/core/backtester.py
+++ b/core/backtester.py
@@ -495,6 +495,19 @@ def trade_risk_dollars(trade: BacktestTrade) -> float | None:
     return abs(trade.entry_price - trade.stop_loss) * trade.qty
 
 
+def initial_risk_pct(trade: BacktestTrade) -> float | None:
+    """The trade's own entry-to-stop distance, as a fraction of entry price.
+
+    This is "1R" in percent terms. Used to scale execution-layer knobs (like
+    profit_lock's arming trigger) to each trade's actual risk instead of a
+    flat percent that means something different for a 5%-stop name than a
+    12%-stop name."""
+    if trade.stop_loss is None or trade.entry_price <= 0:
+        return None
+    risk_pct = abs(trade.entry_price - trade.stop_loss) / trade.entry_price
+    return risk_pct if risk_pct > 0 else None
+
+
 def _realized_daily_returns(
     trades: list[BacktestTrade], initial_capital: float,
 ) -> np.ndarray:
@@ -812,6 +825,30 @@ def _profit_take_level(position: BacktestTrade) -> float | None:
     return position.entry_price * (1.0 + pct)
 
 
+def _resolve_profit_lock_trigger_pct(
+    position: BacktestTrade, trigger_r: float | None,
+) -> float | None:
+    """Convert the configured R-multiple into this trade's own absolute %.
+
+    2026-08-30 paper (24 closed US trades): profit_lock fired on 11/24
+    (46%) at avg +0.21R, while the 2 trades that reached take_profit
+    averaged +2.04R and stop_loss losers averaged -1.07R. Root cause: the
+    old knob (profit_lock_trigger_pct=0.03) armed the ratchet at a flat
+    +3% price move regardless of the trade's stop distance — for a
+    12%-stop swing trade that is only +0.25R, so the profit floor started
+    clamping upside before the trade had even proven itself relative to
+    its own risk, let alone approached its 1.5-3.6R structural target.
+    Scaling the trigger to a multiple of the trade's *own* initial risk
+    (see initial_risk_pct) fixes this for wide- and tight-stop names alike.
+    """
+    if trigger_r is None or trigger_r <= 0:
+        return None
+    risk_pct = initial_risk_pct(position)
+    if risk_pct is None:
+        return None
+    return trigger_r * risk_pct
+
+
 def _profit_lock_level(position: BacktestTrade) -> float | None:
     """Protective floor locking `profit_lock_frac` of peak close-to-close unrl."""
     frac = position.profit_lock_frac
@@ -1513,8 +1550,9 @@ def _core_backtest_symbol(
             open_position.profit_lock_frac = config.get(
                 "profit_lock_frac", ENGINE.profit_lock_frac,
             )
-            open_position.profit_lock_trigger_pct = config.get(
-                "profit_lock_trigger_pct", ENGINE.profit_lock_trigger_pct,
+            open_position.profit_lock_trigger_pct = _resolve_profit_lock_trigger_pct(
+                open_position,
+                config.get("profit_lock_trigger_r", ENGINE.profit_lock_trigger_r),
             )
             i += 1
             continue
@@ -1760,7 +1798,7 @@ class Backtester:
         breakeven_buffer_pct: float = ENGINE.breakeven_buffer_pct,
         profit_take_pct: float | None = ENGINE.profit_take_pct,
         profit_lock_frac: float | None = ENGINE.profit_lock_frac,
-        profit_lock_trigger_pct: float | None = ENGINE.profit_lock_trigger_pct,
+        profit_lock_trigger_r: float | None = ENGINE.profit_lock_trigger_r,
         min_atr_stop_multiple: float | None = ENGINE.min_atr_stop_multiple,
         synthetic_stop_multiple: float = ENGINE.synthetic_stop_multiple,
         atr_stop_floor_multiple: float | None = ENGINE.atr_stop_floor_multiple,
@@ -1829,7 +1867,7 @@ class Backtester:
         self._breakeven_buffer_pct = breakeven_buffer_pct
         self._profit_take_pct = profit_take_pct
         self._profit_lock_frac = profit_lock_frac
-        self._profit_lock_trigger_pct = profit_lock_trigger_pct
+        self._profit_lock_trigger_r = profit_lock_trigger_r
         self._min_atr_stop_multiple = min_atr_stop_multiple
         self._synthetic_stop_multiple = synthetic_stop_multiple
         # Widens (never tightens) a pattern's own stop_loss up to
@@ -1948,7 +1986,7 @@ class Backtester:
             "breakeven_buffer_pct": self._breakeven_buffer_pct,
             "profit_take_pct": self._profit_take_pct,
             "profit_lock_frac": self._profit_lock_frac,
-            "profit_lock_trigger_pct": self._profit_lock_trigger_pct,
+            "profit_lock_trigger_r": self._profit_lock_trigger_r,
             "min_atr_stop_multiple": self._min_atr_stop_multiple,
             "synthetic_stop_multiple": self._synthetic_stop_multiple,
             "atr_stop_floor_multiple": self._atr_stop_floor_multiple,
@@ -2107,7 +2145,7 @@ class Backtester:
                 "breakeven_buffer_pct": self._breakeven_buffer_pct,
                 "profit_take_pct": self._profit_take_pct,
                 "profit_lock_frac": self._profit_lock_frac,
-                "profit_lock_trigger_pct": self._profit_lock_trigger_pct,
+                "profit_lock_trigger_r": self._profit_lock_trigger_r,
                 "min_atr_stop_multiple": self._min_atr_stop_multiple,
                 "synthetic_stop_multiple": self._synthetic_stop_multiple,
                 "atr_stop_floor_multiple": self._atr_stop_floor_multiple,
diff --git a/core/engine_defaults.py b/core/engine_defaults.py
index 2bce4ae..78f25b2 100644
--- a/core/engine_defaults.py
+++ b/core/engine_defaults.py
@@ -77,16 +77,27 @@ class EngineDefaults:
     # 0 / None = off.
     profit_take_pct: float | None = None
     # Ratcheting profit floor: once peak close-to-close unrl (_best_pnl_pct)
-    # clears profit_lock_trigger_pct, protective stop = entry × (1 ± best ×
-    # frac) for long/short. Never loosens as best only rises. Caps giveback
-    # (e.g. +10% MFE → floor at +5% with frac=0.5) without capping upside.
-    # 2026-08-30 paper: arming from the first green tick harvested 1% noise
-    # as "Lock" (+0.3–0.8%) that could not pay for −6% hard stops. 0 / None = off.
+    # clears the trade's own trigger (see profit_lock_trigger_r below),
+    # protective stop = entry × (1 ± best × frac) for long/short. Never
+    # loosens as best only rises. Caps giveback (e.g. +10% MFE → floor at
+    # +5% with frac=0.5) without capping upside. 0 / None = off.
     profit_lock_frac: float | None = 0.5
-    # Do not arm the ratchet until peak unrl reaches this. Same default as
-    # breakeven_trigger_pct so a +3% excursion both scratches via BE and
-    # starts locking 50% of MFE. 0 / None = arm on any positive MFE.
-    profit_lock_trigger_pct: float | None = 0.03
+    # Do not arm the ratchet until peak unrl reaches this many multiples of
+    # the trade's OWN initial risk (entry-to-stop distance, i.e. "R").
+    # 2026-08-30 paper (24 closed US trades): the old knob was a flat
+    # +3% price move (profit_lock_trigger_pct=0.03) regardless of stop
+    # distance. With hard_stop_percentage=0.12 that is only +0.25R — the
+    # floor started ratcheting up before a trade had even earned back a
+    # quarter of what it was risking, let alone approached its 1.5-3.6R
+    # structural target. Result: profit_lock fired on 11/24 trades (46%)
+    # at avg +0.21R while stop_loss losers averaged -1.07R and the 2
+    # trades that reached take_profit averaged +2.04R — winners cut short,
+    # losers run full. Requiring +1R of proof before arming (then still
+    # only locking 50% of whatever peak follows) leaves genuine winners
+    # room to run toward their measured-move target while still trimming
+    # giveback once a trade is unambiguously working. 0 / None = arm on
+    # any positive MFE (the old too-eager behavior).
+    profit_lock_trigger_r: float | None = 1.0
     # Empty on purpose. 006/007 used to skip SMA200 (shorts of strength /
     # longs of weakness) and then dominated the losing paper book. They are
     # disabled by default; --pattern isolation still gets the regime filter.
diff --git a/core/kronos_rank_sleeve.py b/core/kronos_rank_sleeve.py
index d3eaa1e..b495a31 100644
--- a/core/kronos_rank_sleeve.py
+++ b/core/kronos_rank_sleeve.py
@@ -326,6 +326,7 @@ def backtest_rank_sleeve(
         _check_exit,
         _close_trade,
         _open_trade,
+        _resolve_profit_lock_trigger_pct,
         _update_trailing_reference,
         apply_risk_gates,
     )
@@ -420,8 +421,8 @@ def backtest_rank_sleeve(
             pos.breakeven_buffer_pct = config.get("breakeven_buffer_pct", 0.0)
             pos.profit_take_pct = config.get("profit_take_pct", ENGINE.profit_take_pct)
             pos.profit_lock_frac = config.get("profit_lock_frac", ENGINE.profit_lock_frac)
-            pos.profit_lock_trigger_pct = config.get(
-                "profit_lock_trigger_pct", ENGINE.profit_lock_trigger_pct,
+            pos.profit_lock_trigger_pct = _resolve_profit_lock_trigger_pct(
+                pos, config.get("profit_lock_trigger_r", ENGINE.profit_lock_trigger_r),
             )
             open_pos[sym] = pos
 
diff --git a/core/paper_trader.py b/core/paper_trader.py
index 307b540..6c3718b 100644
--- a/core/paper_trader.py
+++ b/core/paper_trader.py
@@ -27,6 +27,7 @@ from core.backtester import (
     _check_exit,
     _close_trade,
     _open_trade,
+    _resolve_profit_lock_trigger_pct,
     _update_trailing_reference,
     _execution_reward_risk_ok,
     trade_r_multiple,
@@ -579,7 +580,9 @@ class PaperAccount:
         position.breakeven_buffer_pct = profile.breakeven_buffer_pct
         position.profit_take_pct = ENGINE.profit_take_pct
         position.profit_lock_frac = ENGINE.profit_lock_frac
-        position.profit_lock_trigger_pct = ENGINE.profit_lock_trigger_pct
+        position.profit_lock_trigger_pct = _resolve_profit_lock_trigger_pct(
+            position, ENGINE.profit_lock_trigger_r,
+        )
         notional = position.entry_price * position.qty
         if signal.action == "BUY":
             self.cash -= notional
@@ -650,8 +653,8 @@ class PaperAccount:
         position.profit_lock_frac = (
             ENGINE.profit_lock_frac if ENGINE.profit_lock_frac else None
         )
-        position.profit_lock_trigger_pct = (
-            ENGINE.profit_lock_trigger_pct if ENGINE.profit_lock_trigger_pct else None
+        position.profit_lock_trigger_pct = _resolve_profit_lock_trigger_pct(
+            position, ENGINE.profit_lock_trigger_r,
         )
         exit_price, reason = _check_exit(
             candle, position, bar_idx, min_hold_bars=ENGINE.min_hold_bars,
@@ -850,9 +853,8 @@ class PaperAccount:
             pos.profit_lock_frac = (
                 ENGINE.profit_lock_frac if ENGINE.profit_lock_frac else None
             )
-            pos.profit_lock_trigger_pct = (
-                ENGINE.profit_lock_trigger_pct
-                if ENGINE.profit_lock_trigger_pct else None
+            pos.profit_lock_trigger_pct = _resolve_profit_lock_trigger_pct(
+                pos, ENGINE.profit_lock_trigger_r,
             )
         acct.closed = [_trade_from_dict(d) for d in data.get("closed", [])]
         raw_curve = [tuple(x) for x in data.get("equity_curve", [])]
diff --git a/core/test_engine_defaults.py b/core/test_engine_defaults.py
index ab777b4..9246df8 100644
--- a/core/test_engine_defaults.py
+++ b/core/test_engine_defaults.py
@@ -51,7 +51,7 @@ def demo():
     assert ENGINE.cooldown_bars == 10
     assert ENGINE.profit_take_pct is None
     assert ENGINE.profit_lock_frac == 0.5
-    assert ENGINE.profit_lock_trigger_pct == 0.03
+    assert ENGINE.profit_lock_trigger_r == 1.0
     from config import DISABLED_PATTERNS
     # 002/004/008 posted 0-25% win rates whenever the regime filter was
     # bypassed (pattern_only paper runs) — disabled by default now.
@@ -119,7 +119,7 @@ def demo():
     assert ENGINE.breakeven_trigger_pct == 0.03
     assert ENGINE.profit_take_pct is None
     assert ENGINE.profit_lock_frac == 0.5
-    assert ENGINE.profit_lock_trigger_pct == 0.03
+    assert ENGINE.profit_lock_trigger_r == 1.0
 
     # 1.5% hysteresis: ~1% the wrong side of SMA200 is a near-miss, not a block.
     buy_near = [100.0] * 200 + [99.0]
@@ -162,7 +162,11 @@ def demo():
     )
 
     rg = risk_gate_kwargs()
-    assert rg["hard_stop_percentage"] == 0.06
+    # NOTE: was asserting 0.06 (the pre-2026-08-30 default). The code moved
+    # to 0.12 (see engine_defaults.py) but this test was never updated, so
+    # it would not have caught a regression back to the tight cap that
+    # caused 32% of a book's trades to be 100%-loser stop_loss exits.
+    assert rg["hard_stop_percentage"] == 0.12
     assert "max_position_pct" not in rg
 
     sk = sizing_kwargs(account_value=50_000.0)
@@ -177,7 +181,7 @@ def demo():
     assert bt["breakeven_trigger_pct"] == 0.03
     assert bt["profit_take_pct"] is None
     assert bt["profit_lock_frac"] == 0.5
-    assert bt["profit_lock_trigger_pct"] == 0.03
+    assert bt["profit_lock_trigger_r"] == 1.0
     assert bt["breakeven_buffer_pct"] == 0.0015
     assert bt["min_share_price"] == 5.0
     assert "regime_hysteresis_pct" not in bt
diff --git a/core/test_paper_trader.py b/core/test_paper_trader.py
index 54bf484..8449832 100644
--- a/core/test_paper_trader.py
+++ b/core/test_paper_trader.py
@@ -367,7 +367,9 @@ def test_us_paper_keeps_engine_breakeven():
         assert pos.breakeven_buffer_pct == 0.0015
         assert pos.profit_take_pct is None
         assert pos.profit_lock_frac == 0.5
-        assert pos.profit_lock_trigger_pct == 0.03
+        # entry=100, stop=94 → risk_pct=0.06; trigger_r=1.0 → resolved
+        # per-trade trigger = 1.0 * 0.06 = 0.06 (must be up 1R before arming).
+        assert pos.profit_lock_trigger_pct == 0.06
     finally:
         settings.min_position_notional = old_min
 
diff --git a/ui/backtest_dialog.py b/ui/backtest_dialog.py
index a3d3f0d..81da985 100644
--- a/ui/backtest_dialog.py
+++ b/ui/backtest_dialog.py
@@ -187,11 +187,14 @@ PARAMS: list[tuple[str, str, str, str, Any, Optional[list[str]]]] = [
         "spin", (ENGINE.profit_lock_frac or 0.0, 0.0, 1.0, 0.05), None,
     ),
     (
-        "profit_lock_trigger_pct", "Profit lock trigger (%)",
-        "Peak unrl required before the profit-lock ratchet arms "
-        "(0.03 = ignore 1% noise; same default as breakeven). "
-        "0 = arm on any green tick (too tight in paper). ",
-        "spin", (ENGINE.profit_lock_trigger_pct or 0.0, 0.0, 0.2, 0.005), None,
+        "profit_lock_trigger_r", "Profit lock trigger (R)",
+        "Peak gain required, in multiples of THIS TRADE's own initial risk "
+        "(entry-to-stop distance), before the profit-lock ratchet arms "
+        "(1.0 = must be up 1x what it's risking first). Scales with each "
+        "trade's stop distance instead of a flat % that meant 0.25R for a "
+        "12%-stop name and 1R for a 3%-stop name. 0 = arm on any green tick "
+        "(too tight in paper — see engine_defaults.py notes).",
+        "spin", (ENGINE.profit_lock_trigger_r or 0.0, 0.0, 3.0, 0.1), None,
     ),
     (
         "breakeven_trigger_pct", "Breakeven trigger (%)",
@@ -483,7 +486,7 @@ class BacktestDialog:
         for opt_key in (
             "breakeven_trigger_pct", "min_atr_stop_multiple",
             "min_reward_risk_ratio", "hard_stop_percentage", "atr_stop_floor_multiple",
-            "profit_take_pct", "profit_lock_frac", "profit_lock_trigger_pct",
+            "profit_take_pct", "profit_lock_frac", "profit_lock_trigger_r",
         ):
             if opt_key in p and p[opt_key] is not None and p[opt_key] <= 0:
                 p[opt_key] = None
diff --git a/web/jobs.py b/web/jobs.py
index 8aca8f4..f0f1c12 100644
--- a/web/jobs.py
+++ b/web/jobs.py
@@ -97,7 +97,7 @@ def normalize_backtest_form(raw: dict[str, Any]) -> dict[str, Any]:
         "atr_stop_floor_multiple",
         "profit_take_pct",
         "profit_lock_frac",
-        "profit_lock_trigger_pct",
+        "profit_lock_trigger_r",
     ):
         if opt_key in p and p[opt_key] is not None and p[opt_key] <= 0:
             p[opt_key] = None
